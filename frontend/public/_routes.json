{
  "version": 1,
  "description": "Route configuration for Cloudflare Pages. Excludes /api/* from static asset serving so that API requests are handled by the backend Workers service.",
  "exclude": ["/api/*"]
}
