{
  "name": "route-atlas",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "dev": "concurrently -n frontend,backend -c blue,green \"pnpm --filter frontend dev\" \"pnpm --filter @route-atlas/backend dev\"",
    "build": "pnpm --filter @route-atlas/shared build && pnpm --filter @route-atlas/backend build && pnpm --filter frontend build",
    "build:shared": "pnpm --filter @route-atlas/shared build",
    "build:backend": "pnpm --filter @route-atlas/backend build",
    "build:frontend": "pnpm --filter frontend build",
    "test": "pnpm --filter @route-atlas/backend test && pnpm --filter frontend test",
    "test:backend": "pnpm --filter @route-atlas/backend test",
    "test:frontend": "pnpm --filter frontend test",
    "lint": "pnpm --filter @route-atlas/shared lint && pnpm --filter @route-atlas/backend lint && pnpm --filter frontend lint",
    "format:check": "prettier --check '**/*.{ts,html,scss,json,md}' --ignore-path .gitignore"
  },
  "devDependencies": {
    "@eslint/js": "^9.25.0",
    "concurrently": "^9.1.0",
    "eslint": "^9.25.0",
    "eslint-config-prettier": "^10.1.0",
    "prettier": "^3.5.0",
    "typescript-eslint": "^8.31.0"
  }
}
